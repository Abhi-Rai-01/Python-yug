name = "Abhirai"
print(len(name))  #len() is function used to count the lenght of the string
print(name[3])   #we are accessing the 3rd index from the string Abhirai which is assigned to name variable.
print(name[-3])      #we are accessing the 3rd index from the end from the string Abhirai which is assigned to name variable.

age = "18"
print(type(age))